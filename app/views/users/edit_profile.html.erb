<%= turbo_frame_tag :user do %>
  <div class="content">
    <div class="max-w-3xl mx-auto">
      <div class="bg-white shadow sm:rounded-lg">
        <div class="px-4 py-5 sm:p-6">
          <h1 class="text-2xl font-bold mb-6">Edit Profile</h1>
          
          <%= simple_form_for @user, url: update_profile_users_path, method: :patch do |f| %>
            <div class="grid grid-cols-1 gap-y-6 gap-x-4 sm:grid-cols-2">
              <%= f.input :first_name,
                        required: true,
                        label: 'First Name',
                        input_html: { class: 'form-input' } %>

              <%= f.input :last_name,
                        required: true,
                        label: 'Last Name',
                        input_html: { class: 'form-input' } %>

              <%= f.input :email,
                        required: true,
                        input_html: { class: 'form-input' } %>
            </div>

            <div class="border-t border-gray-200 mt-6 pt-6">
              <h2 class="text-lg font-medium mb-4">Change Password</h2>
              <div class="grid grid-cols-1 gap-y-6">
                <%= f.input :current_password,
                          label: 'Current Password',
                          required: false,
                          input_html: { class: 'form-input' },
                          hint: 'We need your current password to confirm your changes' %>

                <%= f.input :password,
                          label: 'New Password',
                          required: false,
                          input_html: { class: 'form-input' },
                          hint: 'Leave blank if you don\'t want to change it' %>

                <%= f.input :password_confirmation,
                          label: 'Confirm New Password',
                          required: false,
                          input_html: { class: 'form-input' } %>
              </div>
            </div>

            <div class="mt-6 flex items-center justify-end gap-x-6">
              <%= link_to 'Cancel', profile_users_path, class: "btn btn-secondary" %>
              <%= f.submit 'Save', class: "btn btn-primary", data: { disable_with: 'Saving...' } %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
<% end %> 